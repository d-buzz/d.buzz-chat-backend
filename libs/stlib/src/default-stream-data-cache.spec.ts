import { DefaultStreamDataCache } from './default-stream-data-cache';

describe('DefaultStreamDataCache', () => {
  it('should be defined', () => {
    expect(new DefaultStreamDataCache()).toBeDefined();
  });
});
